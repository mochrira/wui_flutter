import 'package:flutter/material.dart';

class WuiButton extends StatelessWidget {
  const WuiButton({super.key});

  @override
  Widget build(BuildContext context) {
    return RawMaterialButton(
      onPressed: () {

      }
    );
  }
}